export interface ToolInformationDto {
    id?: string,
    name?: string,
    category?: string,
    shortDescription?: string,
    description?: string,
    icon?: any,
    isActive?: boolean,
    isHidden?: boolean,
    background?: string,
}