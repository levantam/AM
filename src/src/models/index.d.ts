export interface ToolInformationDto {
    id?: string,
    name?: string,
    category?: string,
    shortDescription?: string,
    description?: string,
    icon?: string,
    isActive?: boolean,
    background?: string,
}